{
  "name": "shared-expenses-monorepo",
  "version": "1.0.0",
  "description": "Root package.json for Shared Expenses Tracker",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "init:db": "npm run db:init --prefix server",
    "seed:db": "npm run db:seed --prefix server",
    "start:server": "npm run dev --prefix server",
    "start:client": "npm run dev --prefix client"
  }
}
